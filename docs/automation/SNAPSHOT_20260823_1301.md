# DEV5 SNAPSHOT 2026-08-23 13:01 Europe/Kyiv

MODE: SAFE_OVERLAP_COORDINATION / REPAIRED_STAGE1_PROMOTION
CUTOFF: 2026-08-23T13:01:42+03:00

## Authoritative release facts at cutoff
- Prior accepted Stage1: `0fa442330bc2bb03636ff9297512da4c29e38684`.
- Persistent historical exact-GREEN validation anchor: `dd9ebf9414103c805892856fe6a04706fa69039f`.
- DEV4 `3e15dc2e844cb825e482317fd024795130147011` remains prior repair lineage only.
- DEV1 QA-only PR #155 exact head `c23c88ac21a6a9c82fad0de4aeadb695f82c5951` had terminal RED before cutoff: Windows drive-relative `C:Users\\PrivateUser\\Documents\\analysis.pgn` was returned as `C:Users/PrivateUser/Documents/analysis.pgn` instead of basename-only.
- DEV5 PR #151 repaired exactly that proven gap in `acs/report_paths.py` by treating any alphabetic drive-qualified `X:` prefix as private, including drive-relative forms. Product regression tests lock both `C:Users...` and `D:WorkstationOwner...` cases.
- Current PR #151 exact head: `df52aeb3d99f4ae3d0089eab2882fe9b3c373dfd`.
- Exact `DEV5 Stage1 Path Privacy Repair CI` run `32627946799` is terminal SUCCESS. Linux job `97166119460` SUCCESS; Windows job `97166119501` SUCCESS. Both include compile, focused privacy/release contracts, full unittest, full pytest and diagnostic. Linux replays current independent privacy oracles unchanged; Windows replays the PR #155 drive-relative oracle.
- Compare `0fa442...` -> `df52aeb...`: ahead 15, behind 0. Product delta is limited to `acs/engine.py`, `acs/import_registry.py`, `acs/pgn_service.py`, new `acs/report_paths.py`; plus one release CI workflow and one privacy regression test file. No chess state, GameTree, WebView/UI or strict UIA helper delta.

## Promotion ruling
`df52aeb3d99f4ae3d0089eab2882fe9b3c373dfd` is explicitly PROMOTED as the repaired Stage1 Product authority for the next fresh Windows candidate chain. This is a release-lineage authority decision only; it does not merge PR #151 or mutate PR #54/frozen refs.

`0fa442...` remains the prior accepted Stage1 baseline and historical comparison anchor, not the Product SHA to package next.

## UIA/candidate state
V2 still proves the unique original Move Edit and native Backspace `e9 -> e`, then stops before Ctrl+A at immediate SetValue readback. No Ctrl+A Product defect is proven. Existing observability `066d1e254...` and V3 `f13f20ca...` remain historical QA evidence and must not be silently retargeted.

A new candidate requires one explicitly designated QA harness locked to promoted Product `df52aeb...` and the complete uninterrupted Windows machine chain: exact source/frozen identity, release contracts, WAV assets, official Stockfish, native menu structural gate, Nuitka standalone, built-EXE diagnostic, real WebView2 startup, strict UIA interaction, packaged sound/Stockfish lifecycle, release preflight, ZIP reopen/identity and candidate upload.

FRESH_WINDOWS_CANDIDATE=NO
READY_FOR_RELEASE=NO
NVDA_VERIFIED=NO

Rejected ZIP is forbidden. NVDA remains NO until the user personally verifies the exact new candidate.
