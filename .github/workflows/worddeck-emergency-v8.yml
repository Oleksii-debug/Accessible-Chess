name: WordDeck archived emergency Oxford 5000 V8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived status
        run: echo 'Historical V8 completion is archived after the accepted 5446-row corpus and complete British AudioPack. No source or release mutation is permitted.'
