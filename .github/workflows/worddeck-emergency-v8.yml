name: WordDeck archived emergency Oxford 5000 V8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical V8 completion is archived after accepted corpus/audio completion. No source or release mutation is permitted.'
