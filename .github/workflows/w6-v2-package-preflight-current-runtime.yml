name: W6 V2 Package Preflight Current Runtime

on:
  pull_request:
    branches:
      - codex/v2-runtime-completion-20260907
    paths:
      - 'acs/version2_package_preflight.py'
      - 'tests/test_version2_package_preflight.py'
      - 'tests/test_version2_package_preflight_hardening.py'
      - 'tests/test_v2_package_required_resources.py'
      - '.github/workflows/w6-v2-package-preflight-current-runtime.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: w6-v2-package-preflight-current-runtime-${{ github.ref }}
  cancel-in-progress: true

env:
  CURRENT_RUNTIME_BASE: 8610b3a79c1631a9619427a58ae1617cb1576210
  OWNER_511_HEAD: 58be5a7e577714f221be6653655f4bc1d6bb1acd
  OWNER_565_PARENT_HEAD: f9f9ba6915ce88674214365a6260c92c091ebc1b
  OWNER_565_CURRENT_HEAD: 2830fe08f0ad820711ab3ad258f792ba72c58d8c
  OWNER_PREFLIGHT_BLOB: 8237eb297818d401c29af476f4ef6081d6efcee8
  OWNER_TEST_BLOB: d20c858de65947c7aa1e9543da0b355f697ed359
  OWNER_HARDENING_BLOB: 9547cad72a58fe556b9a3aee004c7a513c1b7bcc
  OWNER_REQUIRED_RESOURCES_BLOB: 37636b3dc0721fb5d3c81f1b5acfb9b28260a91d

jobs:
  focused:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Prove exact current-base selective convergence
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor "$CURRENT_RUNTIME_BASE" HEAD
          git diff --check "$CURRENT_RUNTIME_BASE" HEAD
          changed="$(git diff --name-only "$CURRENT_RUNTIME_BASE" HEAD | sort)"
          expected="$(printf '%s\n' \
            '.github/workflows/w6-v2-package-preflight-current-runtime.yml' \
            'acs/version2_package_preflight.py' \
            'tests/test_version2_package_preflight.py' | sort)"
          test "$changed" = "$expected" || {
            printf 'Unexpected convergence delta:\n%s\n' "$changed" >&2
            exit 1
          }
          git cat-file -e "$OWNER_565_CURRENT_HEAD^{commit}"
          git merge-base --is-ancestor "$OWNER_565_PARENT_HEAD" "$OWNER_565_CURRENT_HEAD"
          test "$(git rev-parse HEAD:acs/version2_package_preflight.py)" = "$OWNER_PREFLIGHT_BLOB"
          test "$(git rev-parse HEAD:tests/test_version2_package_preflight.py)" = "$OWNER_TEST_BLOB"
          test "$(git rev-parse HEAD:tests/test_version2_package_preflight_hardening.py)" = "$OWNER_HARDENING_BLOB"
          test "$(git rev-parse HEAD:tests/test_v2_package_required_resources.py)" = "$OWNER_REQUIRED_RESOURCES_BLOB"
          test "$(git rev-parse "$OWNER_565_CURRENT_HEAD":acs/version2_package_preflight.py)" = "$OWNER_PREFLIGHT_BLOB"
          test "$(git rev-parse "$OWNER_565_CURRENT_HEAD":tests/test_version2_package_preflight.py)" = "$OWNER_TEST_BLOB"
          test "$(git rev-parse "$OWNER_565_CURRENT_HEAD":tests/test_v2_package_required_resources.py)" = "$OWNER_REQUIRED_RESOURCES_BLOB"
          test "$(git rev-parse "$OWNER_511_HEAD":tests/test_version2_package_preflight_hardening.py)" = "$OWNER_HARDENING_BLOB"
          echo 'CURRENT_RUNTIME_BASE + EXACT CURRENT #565/#511 PREFLIGHT OWNER BYTES PASS'

      - name: Prove one canonical V2 package validator
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          candidates = sorted(
              p.as_posix()
              for p in Path('acs').glob('*.py')
              if ('version2_package_preflight' in p.name or 'version2_package_validator' in p.name)
          )
          assert candidates == ['acs/version2_package_preflight.py'], candidates
          assert Path('acs/release_preflight.py').exists(), 'Stage1 preflight remains separate but must not replace V2 preflight'
          print('ONE_CANONICAL_V2_PACKAGE_VALIDATOR', candidates[0])
          PY

      - name: Compile validator and current production contracts
        run: >-
          python -m compileall -q
          acs/version2_package_preflight.py
          acs/acsdb.py
          acs/settings.py
          acs/version2_upgrade.py
          acs/sound_events.py
          acs/sound_windows.py
          acs/stockfish_runtime.py
          acs/version2_release_ui.py
          tests/test_version2_package_preflight.py
          tests/test_version2_package_preflight_hardening.py
          tests/test_v2_package_required_resources.py

      - name: Current schema and resource identity smoke
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from acs.acsdb import ACSDB_SCHEMA_VERSION
          from acs.settings import SCHEMA_VERSION as SETTINGS_SCHEMA_VERSION
          from acs.sound_events import SoundEvent
          from acs.sound_windows import DEFAULT_SOUND_MANIFEST, DEFAULT_SOUND_RELATIVE_DIR, SOUND_MANIFEST_SCHEMA_VERSION
          from acs.stockfish_runtime import PACKAGED_STOCKFISH_RELATIVE_PATH
          from acs.version2_package_preflight import V2_PACKAGE_MANIFEST_SCHEMA_VERSION, V2_PACKAGE_PROFILE
          from acs.version2_upgrade import UPGRADE_JOURNAL_SCHEMA_VERSION

          assert type(ACSDB_SCHEMA_VERSION) is int and ACSDB_SCHEMA_VERSION > 0
          assert type(SETTINGS_SCHEMA_VERSION) is int and SETTINGS_SCHEMA_VERSION > 0
          assert type(UPGRADE_JOURNAL_SCHEMA_VERSION) is int and UPGRADE_JOURNAL_SCHEMA_VERSION > 0
          assert V2_PACKAGE_MANIFEST_SCHEMA_VERSION == 1
          assert V2_PACKAGE_PROFILE == 'version2-default'
          assert SOUND_MANIFEST_SCHEMA_VERSION == 1
          assert DEFAULT_SOUND_RELATIVE_DIR.as_posix() == 'assets/sounds'
          assert DEFAULT_SOUND_MANIFEST == 'manifest.json'
          assert PACKAGED_STOCKFISH_RELATIVE_PATH.as_posix() == 'engines/stockfish/stockfish.exe'
          assert {event.value for event in SoundEvent} == {
              'move', 'capture', 'check', 'castle', 'promotion', 'illegal', 'start', 'end', 'tick'
          }
          print('CURRENT V2 PACKAGE CONTRACT IDENTITY PASS')
          PY

      - name: Canonical V2 tree ZIP and required-resource regressions
        run: >-
          python -m unittest -v
          tests.test_version2_package_preflight
          tests.test_version2_package_preflight_hardening
          tests.test_v2_package_required_resources

  broad:
    runs-on: ubuntu-22.04
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install pinned verification dependency
        run: python -m pip install -q pytest==8.4.2
      - name: Full unittest
        run: python -m unittest discover -s tests -v
      - name: Full pytest
        run: python -m pytest -q tests
      - name: Exact diagnostics
        run: |
          python -m acs.selftest
          python run_accessible_chess.py --diagnostic
          python run_accessible_chess_v2.py --diagnostic
