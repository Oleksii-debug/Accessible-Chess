name: WordDeck historical archive - completed 1156 integration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The user-approved 1156-row Oxford 5000 integration is already accepted and complete. This historical workflow is intentionally read-only; original integration logic remains in Git history."
