name: WordDeck archived emergency Oxford 5000 V5 strict

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived status
        run: echo 'Historical V5 strict completion is archived after the accepted 5446-row corpus. No source mutation is permitted.'
