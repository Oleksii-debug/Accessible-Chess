# DEV5-2257 DIRECTIVE

Cutoff authority: 2026-08-23T19:57:48Z.

S1-01/D04: close #249 only on the owned release-preflight Product surface. Reject duplicate JSON object keys recursively before semantic validation. Keep the #249 oracle unchanged and preserve #218/#228/#239 behavior. Publish terminal exact-head Linux+Windows evidence with full suites and exact scope/provenance locks.

S1-02/D05: do not recompute or move Stage1 authority until the S1-01 repair is terminal and independently accepted. Then selectively intake the cumulative D04 Product/test delta, not evidence workflows wholesale, and run one new exact combined dual-OS gate with immutable Stage1 oracles and full release/accessibility regressions.

S1-05: do not create or retarget any Windows candidate until exact recomposed SHA is accepted and shared authority advances with `force=false`. Old V5/rejected ZIPs are forbidden. Future candidate WIP=1 only.

DEV5: remain SAFE OVERLAP while these touching owners are active. Review evidence, ancestry, conflicts and ordering; do not compete on Product.

NVDA_VERIFIED remains NO until the user personally verifies the exact fresh machine-GREEN candidate.
