name: WordDeck archived emergency Oxford 5000 completion

on:
  workflow_dispatch:

# Historical recovery logic is preserved in Git history. The accepted corpus is
# complete, so this workflow is an inert marker and cannot mutate source.
permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived after accepted corpus completion. No source, audio, branch or release mutation is performed.'
