name: WordDeck archived emergency Oxford 5000 V8b

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived status
        run: echo 'Historical V8b is archived after accepted V0.1. No lexical, branch or release mutation is performed.'
