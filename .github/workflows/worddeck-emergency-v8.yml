name: WordDeck historical archive - emergency v8

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical WordDeck emergency v8 is archived read-only. Original logic remains in Git history."
