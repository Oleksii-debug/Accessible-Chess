# Accessible Chess — Autonomous Routing Control

EPOCH: CHESS-2026-09-07-04
STATE: ACTIVE — FIVE HOURLY WORKERS

## Operating invariant
Every worker, Codex Cloud run and Work run must reconstruct the newest live state before substantive work. Prefer current PR/CI/runtime evidence over stale narrative status. Classify inherited targets as KEEP, CHANGE, STOP_STALE, COLLISION or PROMOTE. Do not duplicate active ownership.

## Product priority
Converge Version 2 formats/library/books/workflows into one genuinely usable Windows/NVDA release candidate without violating Stage 1 accessibility/release boundaries.

Canonical dependency order:
GameTree/PGN -> ACSDB/Library -> ChessBase adapters -> Books/Training -> later Teacher/Classroom/Web.

## Active convergence lineage
PR #435 `V2: complete format integration and production Windows composition` remains the only active umbrella/convergence target. Do not create a competing umbrella unless fresher evidence explicitly supersedes it.

Already integrated / DO NOT REPEAT:
- W2 #436: canonical bounded PGN ingress for BookTraining.
- W3 #437: dirty-PGN open/discard guard preventing silent data loss.
- W4 #438: V2 UI semantic/accessibility CI expansion while preserving the human NVDA boundary.
- W3 #439: Windows file-workflow action dispatch fails closed off the owning UI thread; terminal Ubuntu + Windows semantic evidence preceded intake into #435.
- W3 #443: dirty-PGN discard confirmation is bound to the validated application owner window, preserves Yes/No behavior, and fails closed before any unowned MessageBox. Focused UI Semantic evidence was GREEN before history-preserving convergence and intake into #435.

STOP_STALE / do not revive:
- #434: predecessor Windows/NVDA-format convergence PR. #435 is its explicit successor and contains the accepted lineage; #434 is closed.
- W4 #440: old convergence-aware Windows-composition child; equivalent repair landed directly on #435.
- W4 #442: old automated Windows/NVDA QA child; superseded and closed after the runtime added the actual V2 release bootstrap and exposed a newer accessibility regression.

Active temporary reservations / collision boundaries:
- #441 owns `Version2Application`, PGN selected-position/export command composition, detached Library game opening, import observer/terminal UI, Book workspace binding, the V2 release UI/bootstrap and actual Windows launcher/application composition. It is moving; do not rewrite or rebase this branch from another worker while its owner is active.
- #444 is W1's active canonical PGN nested-comment resource-preflight convergence package. Do not duplicate its parser/preflight scope.
- #445 is W4's current accessibility/package-QA child of #441. It owns the V2 navigation verbosity/language repair plus convergence-aware Windows/NVDA QA. #442 must not be intaken.

## W5 integration-gate repair — 2026-09-07
Convergence exposed three historical CI topology assertions that became invalid after legitimate child intake. They failed before Product tests, not because Product semantics regressed:
- D01 PGN Workspace WebView historical isolated-scope assertion;
- V2 Windows Composition historical exact-topology assertion;
- broad V2 Formats Integration historical exact changed-file allowlist.

The first two were repaired earlier on #435 while retaining ancestry, frozen Stage 1, protected-core and real Product tests.

The broad Formats gate was then independently proven RED on Ubuntu and Windows at `Prove Version 2 ancestry scope and Stage 1 isolation`: it compared the modern convergence tree to one frozen historical file list. W5 removed only that obsolete equality requirement. The gate still retains:
- accepted V2 and Library-v6 ancestry;
- frozen Stage 1 byte checks;
- strict-release contamination rejection;
- canonical chess/GameTree/PGN/Books protected-path isolation;
- exact integrated owner blob locks;
- DOM contracts;
- focused format/Library/security/cancellation suites;
- full unittest + pytest + SELFTEST/diagnostic on Ubuntu and Windows;
- the independent real pinned libcbh/uncbv CBH/CBV journey.

After #443 intake, current #435 exact head has terminal GREEN UI Semantic Gate. New exact-head broad Formats, V2 Windows Composition, D01 PGN Workspace and Books/Training UI runs are queued/running. Previous parent-head runs cancelled by concurrency are not failures.

## Current release gates
1. Current exact-head #435 CI must reach terminal GREEN across broad formats/books/PGN/Windows/accessibility workflows after the convergence-aware repairs.
2. Any genuine regression that appears after topology/ancestry checks pass must be isolated and repaired on the canonical lineage, not hidden by weakening assertions.
3. Converge active #441 application/launcher user journeys with all accepted #435 children without losing histories or tests.
4. Intake #445 only after its current merge-preview/runtime QA is terminal and its base is current; do not use superseded #442 evidence.
5. Produce one fresh Windows candidate/package and exact-machine packaged runtime evidence; never reuse a previously rejected ZIP.
6. Human NVDA acceptance remains open. Only Oleksii can close it on the exact fresh candidate. `NVDA_VERIFIED=NO` until then.
7. ChessBase-family capability truth must remain explicit SUPPORTED/PARTIAL/UNSUPPORTED/BLOCKED; unsupported formats must not hold the desktop release hostage.

## Worker routing
### W1 — Formats / GameTree / interoperability
KEEP. #444 is the active W1 package. Finish/review that exact canonical nested-comment preflight convergence; do not start a duplicate preflight/parser implementation. After it terminalizes, take the next unowned interoperability/evidence gap.

### W2 — Library / database / books / training content
KEEP with #436 complete. Do not repeat it. Take only an unowned preservation/data-loss, Library/Books/Training linkage, recovery/cancellation or integration-evidence package. Avoid #441-owned Library/import/Book application composition.

### W3 — User workflows / desktop composition
KEEP. #437/#439/#443 are complete and DO NOT REPEAT. Avoid #441 launcher/application/import observer/release UI and #445 accessibility QA. Take the next disjoint user-journey defect only if it can be completed without crossing those active reservations; otherwise help review/integrate accepted children into #435.

### W4 — Windows / NVDA / accessibility / package QA
PROMOTE. #445 is the current line. #442 is STOP_STALE. Finish the V2 navigation verbosity/language repair and convergence-aware Windows/NVDA/package QA; never claim human NVDA verification.

### W5 — Integration / release / coordination
PROMOTE. Watch the new exact-head #435 broad gate after the stale allowlist repair, classify only failures that reach real Product tests, then converge terminal #441/#445 without spawning another umbrella. Close/route stale predecessor lines when exact successor evidence proves them superseded.

## Codex Cloud / Work
Codex Cloud: take only a disjoint unowned exact-head CI/Product integration failure or bounded release-candidate composition package. No second umbrella.

Work: review/implement a high-value cross-layer user-journey or release-evidence package against current #435 that is disjoint from #441/#444/#445 and W1-W5 reservations. Persist durable results.

## Hard boundaries
- One canonical chess core; no chess rules inside PGN/ChessBase/books/database/UI/Web adapters.
- No duplicate umbrella/convergence implementation.
- No reuse of rejected Windows ZIPs.
- `NVDA_VERIFIED=NO` until Oleksii personally tests the exact fresh candidate.
- No paid services/compute without explicit owner approval.
- No silent invention/drop of source data.

## Collision control
Before implementation, check current/recent PRs, issues/reports, CI and worker/Codex/Work results. If another active owner has the same semantic package, integrate/review it or take the next unowned package. Completed #436/#437/#438/#439/#443 and the three convergence-aware CI repairs are DO NOT REPEAT. #434/#440/#442 are STOP_STALE. #441/#444/#445 are live reservations at this checkpoint.

## Next audit trigger
Audit immediately if the new exact-head #435 broad Formats Integration becomes materially GREEN/RED, #441 stops moving or reaches an integration checkpoint, #444/#445 terminalize, a fresh Windows candidate appears, ownership collides/stales, or routing contradicts fresher evidence. Otherwise perform the normal broader coordination audit after the current worker wave.
