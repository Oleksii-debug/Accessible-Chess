name: Remappable PGN Tree Navigation

on:
  pull_request:
    branches:
      - fix/full-product-native-menu-binding-refresh-20260926
    paths:
      - 'acs/keybindings.py'
      - 'acs/full_product_actions.py'
      - 'web/index.html'
      - 'web/full_product_pgn.js'
      - 'tests/test_full_product_pgn_keybindings.py'
      - 'tests/js/pgn_surface_dom_test.js'
      - '.github/workflows/remappable-pgn-tree-navigation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: remappable-pgn-tree-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Prove exact stacked scope
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          base='${{ github.event.pull_request.base.sha }}'
          merge_base="$(git merge-base "$base" HEAD)"
          changed="$(git diff --name-only "$merge_base" HEAD | sort)"
          expected="$(printf '%s\n' \
            '.github/workflows/remappable-pgn-tree-navigation.yml' \
            'acs/full_product_actions.py' \
            'acs/keybindings.py' \
            'tests/js/pgn_surface_dom_test.js' \
            'tests/test_full_product_pgn_keybindings.py' \
            'web/full_product_pgn.js' \
            'web/index.html' | sort)"
          if [ "$changed" != "$expected" ]; then
            printf 'Unexpected PGN remap scope:\n%s\n' "$changed" >&2
            exit 1
          fi

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12.10'

      - name: Compile registry regression
        run: >-
          python -m py_compile
          acs/full_product_actions.py
          acs/keybindings.py
          tests/test_full_product_pgn_keybindings.py

      - name: Prove registry defaults and persisted remap
        run: python -m unittest -v tests.test_full_product_pgn_keybindings

      - name: Prove PGN DOM keyboard and privacy contract
        run: node tests/js/pgn_surface_dom_test.js

      - name: Reject literal PGN navigation key ownership
        shell: bash
        run: |
          set -euo pipefail
          if grep -E 'event\.key[[:space:]]*===?[[:space:]]*"Arrow(Up|Down|Left)"' web/full_product_pgn.js; then
            echo 'PGN surface still owns literal navigation keys' >&2
            exit 1
          fi
