name: WordDeck archived emergency Oxford 5000 V8b

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical V8b completion is archived after accepted V0.1. No lexical, branch or release mutation is performed.'
