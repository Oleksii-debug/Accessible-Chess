name: WordDeck historical archive - emergency completion

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical WordDeck emergency completion workflow is archived read-only. Original implementation remains in Git history and must not mutate accepted source or releases."
