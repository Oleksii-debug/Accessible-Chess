name: WordDeck archived emergency Oxford 5000 completion

on:
  workflow_dispatch:

# Historical recovery logic is preserved in Git history. The accepted corpus is
# now complete (5446 Recall rows; Oxford 5000 additions 2138/2138), so this
# workflow must never mutate the canonical branch again.
permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived status
        run: echo 'Archived after accepted V0.1. No lexical, audio, branch or release mutation is performed.'
