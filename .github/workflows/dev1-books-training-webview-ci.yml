name: DEV1 Books Training WebView CI

on:
  workflow_dispatch:
  push:
    branches:
      - full5/dev1-books-training-webview-20260822-2249
  pull_request:
    branches:
      - full5/dev1-library-webview-20260822-2236

permissions:
  contents: read

jobs:
  dev1-books-training-webview:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.EXPECTED_SHA }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install verification dependency
        run: python -m pip install -q pytest
      - name: Prove exact source head and isolated DEV1 scope
        shell: bash
        run: |
          set -euo pipefail
          base='e358792a26c6d821c35fd99db426aeb3c056bff4'
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          git cat-file -e "$base^{commit}"
          git merge-base --is-ancestor "$base" HEAD
          test "$(git merge-base "$base" HEAD)" = "$base"
          git diff --check "$base" HEAD
          changed="$(git diff --name-only "$base" HEAD)"
          printf '%s\n' "$changed"
          allowed='^(.github/workflows/dev1-books-training-webview-ci.yml|.github/workflows/dev1-full-product-ui-ci.yml|acs/book_webview_projection.py|acs/book_webview_bridge.py|acs/training_webview_projection.py|acs/training_webview_bridge.py|web/full_product_books_training.js|tests/test_dev1_books_training_webview_projection.py|tests/test_dev1_books_training_webview_bridge.py|tests/test_dev1_books_training_web_asset.py|tests/test_dev1_books_training_webview_atomicity.py)$'
          unexpected="$(printf '%s\n' "$changed" | grep -Ev "$allowed" || true)"
          if [ -n "$unexpected" ]; then
            echo 'Unexpected path outside isolated DEV1 Books/Training package:' >&2
            printf '%s\n' "$unexpected" >&2
            exit 1
          fi
          for protected in \
            acs/bookdocument.py \
            acs/bookreader.py \
            acs/training.py \
            acs/full_product_presenters.py \
            acs/full_product_actions.py \
            acs/full_product_ui_shell.py \
            acs/full_product_webview_adapter.py \
            acs/library_webview_projection.py \
            acs/pgn_webview_projection.py; do
            if printf '%s\n' "$changed" | grep -Fx "$protected"; then
              echo "Accepted/backend path churned without a concrete defect: $protected" >&2
              exit 1
            fi
          done
      - name: Parse browser asset
        run: node --check web/full_product_books_training.js
      - name: Compile Product presentation and tests
        run: python -m compileall -q acs tests run_accessible_chess.py
      - name: DEV1 Books Training focused tests
        run: >-
          python -m unittest -v
          tests.test_dev1_books_training_webview_projection
          tests.test_dev1_books_training_webview_bridge
          tests.test_dev1_books_training_web_asset
          tests.test_dev1_books_training_webview_atomicity
          tests.test_dev1_full_product_ui_packages
          tests.test_bookdocument
          tests.test_bookreader
          tests.test_training
      - name: Full repository unittest discovery
        run: python -m unittest discover -s tests -v
      - name: Full repository pytest without weakening
        run: python -m pytest -q
      - name: Complete product diagnostic
        run: python run_accessible_chess.py --diagnostic
