# DEV3 NEXT WORK

Canonical Audit status remains ACTIVE / STAGE1 RELEASE FREEZE / FRESH WINDOWS CANDIDATE PRIORITY. Do not start another DEV3 Full Product feature/Library/Search expansion wave while the fresh Windows candidate chain is unresolved.

Accepted Stage1 source authority:
`manual5/integration-20260821 @ 0fa442330bc2bb03636ff9297512da4c29e38684`.

Highest DEV3 priority on every continuation:
1. re-read DEV5 PR #139 exact head, workflow runs, jobs, logs and artifacts;
2. re-read `qa/dev5-stage1-uia-setvalue-observability-20260823` and determine whether the QA-owned restore/convergence repair has actually materialized;
3. if a corrected candidate rerun exists, inspect exact automated progression through strict UIA interaction, packaged Stockfish/sound lifecycle, release preflight, ZIP reopen/identity and artifact upload;
4. preserve DEV3 exact Windows runtime evidence `32600115025 / 97097006614` as supporting evidence only; never use it to claim a candidate artifact exists;
5. classify any new RED before repair. Only a concrete defect in DEV3-owned Stockfish runtime, analysis, clocks or engine lifecycle may justify a DEV3 Product patch during freeze;
6. if no such defect is proven, remain SAFE OVERLAP and provide non-conflicting evidence/backlog refinement only;
7. do not weaken strict Ctrl+A/Ctrl+C assertions to bypass the current SetValue observability blocker.

The current QA blocker occurs before native Ctrl+A is sent, so it is not evidence of a Product Ctrl+A/Ctrl+C defect. A correct QA-owned repair should reacquire/refetch the original Move Edit after `SetValue`, use bounded fail-closed convergence on the actual ValuePattern value, report drift/timeout truthfully, and then run the unchanged native selection/clipboard assertions.

Current terminal Full Product package PR #137 remains technically GREEN for later selective DEV5 intake. Parallel DEV3 PR #134 independently closes final-review history identity and must not be duplicated.

Do not modify DEV1 UI/WebView Product, DEV2 canonical GameTree/domain, DEV4 PGN/ChessBase/import/security, DEV5 integration/promotion, QA-owned strict Windows harness, frozen Stage1 refs, or rejected archives unless ownership explicitly changes.

Release success requires one fresh Windows archive from exact accepted Stage1 source whose complete automated chain is GREEN and whose artifact identity is verified. Human NVDA verification comes only after that exact artifact is available.

FRESH_WINDOWS_CANDIDATE=NO
NVDA_VERIFIED=NO
