# DEV3 NEXT WORK

Start every next DEV3 wave with a fresh live ownership read. Preserve all terminal packages unless a concrete defect is proven, and enter SAFE OVERLAP whenever another worker or earlier DEV3 wave owns touching Product paths.

Highest safe DEV3 priorities after validated Product head `654679e6f0ecba119b61aaeba9267a815bf8cd10`:
1. bound `EngineGameHandoff(ANALYZE_CURRENT_GAME)` FEN to the shared 512-character engine contract, but only after confirming no earlier same-lane owner; preserve the stable `requires fen` compatibility string and reject oversize before downstream analysis routing;
2. continue engine lifecycle/cancellation/recovery/resource-bound audits on backend-only paths that do not overlap DEV1 UI, DEV2 GameTree/domain, DEV4 import/shared ACSDB, or DEV5 integration;
3. resume Unicode ACSDB sidecar Product migration only after DEV4 shared `acsdb.py` ownership clears and DEV5 authorizes the shared-path reconciliation; existing sidecar evidence remains design authority, not Product schema authority;
4. mistake/blunder analytics only after authoritative StudentGame/Assignment actor identity and a fixed evaluation-perspective contract are terminal.

Do not edit DEV2 canonical GameTree/domain semantics, DEV4 PGN/ChessBase/import security or active shared ACSDB repair, DEV1 UI/WebView models, DEV5 integration/promotion, or frozen Stage1 release lineage. Never create a second canonical chess/application state model.

For each package: focused adversarial tests, broad unittest/pytest, diff/compile, applicable diagnostic, exact observable CI, recoverable commits, and repo RUN_STATE/current/next/session synchronization. Preserve initial RED evidence instead of weakening tests.

FRESH_WINDOWS_CANDIDATE=NO
NVDA_VERIFIED=NO
