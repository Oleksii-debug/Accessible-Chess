# DEV3 NEXT WORK

Canonical priority remains STAGE1 RELEASE FREEZE / exact final authority before one fresh Windows candidate.

Accepted Stage1 authority remains `manual5/integration-20260821 @ 1e9d23b034e6d347fe03c3581469a07e16037c55`. DEV5 PR #195 is only an intermediate combined RELEASE_HOLD surface; current live head `5e8ca72f7dd552ee151ebd5b85c52148004ac307`. DEV2 PR #199 owns the separate oversized-FEN-counter fail-closed defect. No V6/fresh candidate is authorized until all required narrow repairs are reconciled and accepted.

Highest DEV3 priority on every continuation:
1. fresh-read AUTOPULSE D01 PR #197, DEV5 QA oracle PR #196, DEV2 PR #199, DEV5 combined PR #195 and accepted integration ref before any action;
2. do not create a competing UI focus Product repair. PR #197 is the active owner;
3. current PR #197 head `53fff92a18c22a1a555583203461f7b0214041ff` is RED despite focused `61/61 PASS`: run `32638711537`, Ubuntu `97192322458`, Windows `97192322603`; full suite fails three existing packaged resource/composition contracts because the sidecar adds a third resource/evaluate stage;
4. do not weaken those existing contracts. The preferred minimal repair is to keep the existing bootstrap + board-bridge packaged resource surface and explicitly replace the already-installed `baseSubmit` listener with `wrappedSubmit` inside `installMoveFocusPolicy()`;
5. replay independent DEV5 PR #196 oracle unchanged on every new Product head. It must prove `removeEventListener('click', baseSubmit)` before `addEventListener('click', wrappedSubmit)` on the real move-submit element;
6. require focused listener/focus tests plus full unittest, full pytest, canonical selftest and complete diagnostic GREEN on both Ubuntu and Windows before classifying the Product repair as technically ready;
7. preserve V5 strict native evidence already GREEN: original Move Edit classification A, Backspace e9->e, Ctrl+A selection/delete, Ctrl+A/Ctrl+C clipboard e9, 64 squares and board-origin a3 focus. Do not reopen or weaken these assertions;
8. preserve Enter behavior, one canonical move submission, original Move Edit identity, focus-cancellation semantics and no second UI/core path;
9. after Product repair is GREEN, DEV5/AUDIT must selectively reconcile it with the final combined Stage1 source together with DEV2 FEN containment, rerun exact combined privacy/history/FEN/UI contracts and accept the exact final SHA before any candidate;
10. only then may one fresh fail-closed Windows candidate run: source identity/full regressions/privacy -> strict packaged UIA -> packaged Stockfish/sound -> release preflight -> ZIP reopen/hash/identity -> artifact upload;
11. no V5 artifact is a candidate ZIP. NVDA stays unverified until the user personally verifies the exact final artifact;
12. DEV3 may reopen Product code only for a newly proven DEV3-owned Stockfish/runtime/analysis/clock/lifecycle defect.

Promoted privacy remains independently GREEN in DEV3 PR #192 run `32638464690`; real Stockfish 18 support remains GREEN in PR #176. Terminal Full Product PR #137 remains technically GREEN for its isolated later-intake slice.

SAFE_OVERLAP=YES
DEV3_PRODUCT_PATCH_REQUIRED=NO
SUBMIT_FOCUS_OWNER=PR197
PR197_CURRENT_FULL_CI=RED
FRESH_WINDOWS_CANDIDATE=NO
NVDA_VERIFIED=NO
