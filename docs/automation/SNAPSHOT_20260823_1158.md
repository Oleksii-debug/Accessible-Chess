# DEV5 SNAPSHOT 20260823-1158

CUTOFF: 2026-08-23T11:58:14+03:00
MODE: SAFE_OVERLAP_COORDINATION / RELEASE_PRIVACY_REPAIR_VALIDATION

## Authorities
- Accepted Stage1: `0fa442330bc2bb03636ff9297512da4c29e38684`.
- Persistent exact-GREEN Full Product validation authority: `dd9ebf9414103c805892856fe6a04706fa69039f`.
- DEV4 `3e15dc2e844cb825e482317fd024795130147011`: prior repair lineage only, not fully clean privacy authority.

## DEV5 touching owner
Draft PR #151 `release/dev5-stage1-path-privacy-repair-20260823` remains the sole release-critical Product owner for the current Stage1 privacy repair. Current exact head is `c0169ed276fff893f90f85192416612f3b998b5a`.

Exact machine evidence: workflow `DEV5 Stage1 Path Privacy Repair CI`, run `32627628145`, exact head `c0169ed...`, terminal `SUCCESS`. The current repair includes present ImportRegistry batch/OSError protection and current independent oracle replay, so obsolete head `909d8e...` must not be used as promotion authority.

## Remaining release-privacy gate
DEV1 QA-only PR #155 existed before cutoff and targets exact repair `c0169ed...` with valid Windows drive-relative path forms such as `C:Users\\PrivateUser\\Documents\\analysis.pgn`. Exact QA head: `c23c88ac21a6a9c82fad0de4aeadb695f82c5951`. Terminal verdict is not available in current readback. No inference of GREEN or RED is allowed.

DEV3 PR #156 is explicitly superseded by PR #155 and should not be consumed as duplicate authority.

Therefore: PR #151 is exact-CI GREEN but accepted Stage1 remains `0fa442...` pending the independent drive-relative verdict and explicit promotion decision.

## UIA / release candidate
Prior fresh V2 machine evidence proved the unique original Move Edit and native Backspace `e9 -> e`, then stopped before Ctrl+A because immediate `ValuePattern.SetValue` readback was not observed. No Ctrl+A Product defect is proven. QA observability `066d1e254...` and prepared V3 `f13f20ca...` are untouched.

No fresh Windows candidate exists. Old PR #139 artifact state and rejected ZIP are invalid for reuse.

## Ordering
1. Read terminal PR #155 verdict.
2. If GREEN: independently verify exact `c0169ed...` diff/current oracles, then explicitly promote repaired Stage1.
3. If RED: repair only the proven drive-relative sanitizer boundary, preserving safe-relative provenance and all existing privacy assertions; rerun complete current Linux+Windows privacy/full/release CI and independent oracle.
4. Only after explicit promotion: execute one new uninterrupted Windows candidate chain through exact source/frozen identity, release contracts, WAV, official Stockfish, native menu, Nuitka, built EXE diagnostic, real WebView2, strict UIA, packaged sound/Stockfish lifecycle, preflight, ZIP reopen/identity and artifact upload.
5. Human NVDA verification applies only to that exact newly produced candidate.

## Freeze and coordination
For DEV1-DEV4 coordination, only terminal evidence existing before this cutoff is admissible. Post-cutoff lane output cannot be retroactively consumed into this snapshot.

DEV2/DEV3 Full Product feature slices remain later selective backlog and are excluded from Stage1 release composition.

PR #54 and frozen refs remain untouched. No force-push, test weakening, skip or xfail. `AGENTS.md` and `docs/codex/*` remain absent on live default branch; versioned `docs/automation/*` plus live GitHub evidence are coordinator truth.

FRESH_WINDOWS_CANDIDATE=NO
READY_FOR_RELEASE=NO
NVDA_VERIFIED=NO
