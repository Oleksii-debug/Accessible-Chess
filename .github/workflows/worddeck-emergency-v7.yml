name: WordDeck archived emergency Oxford 5000 V7

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical V7 completion is archived after the accepted corpus. No source mutation is permitted.'
