name: WordDeck historical archive - accepted V0.1 package

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Accepted WordDeck V0.1 is an immutable historical release. This workflow no longer rebuilds later source under the V0.1 name; original package logic remains in Git history."
