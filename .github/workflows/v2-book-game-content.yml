name: V2 Book Canonical Game Content

on:
  push:
    branches:
      - work/v2-book-canonical-game-content-20260828
  pull_request:
    branches:
      - work/version2-integration-formats-20260827
    paths:
      - 'acs/book_game_content.py'
      - 'tests/test_v2_book_game_content.py'
      - '.github/workflows/v2-book-game-content.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: v2-book-game-content-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 55
    env:
      PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Preserve committed Git bytes for cross-platform blob identity gates
        shell: bash
        run: |
          set -euo pipefail
          git config core.autocrlf false
          git config core.eol lf
          git checkout-index --force --all

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install verification dependency
        run: python -m pip install -q pytest==8.4.2

      - name: Prove current-base ancestry and disjoint Books GameTree scope
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            upstream="$PR_BASE_SHA"
            mode="current-pr"
          elif git rev-parse --verify HEAD^2 >/dev/null 2>&1; then
            upstream="$(git rev-parse HEAD^2)"
            mode="merge-sync-push"
          else
            upstream="$(git rev-parse HEAD^)"
            mode="incremental-push"
          fi
          test -n "$upstream"
          git merge-base --is-ancestor "$upstream" HEAD
          test "$(git merge-base "$upstream" HEAD)" = "$upstream"
          git diff --check "$upstream" HEAD
          changed="$(git diff --name-only "$upstream" HEAD | sort)"
          test -n "$changed"

          while IFS= read -r path; do
            case "$path" in
              .github/workflows/v2-book-game-content.yml|acs/book_game_content.py|tests/test_v2_book_game_content.py)
                ;;
              *)
                echo "Unexpected path outside Book canonical game ownership: $path" >&2
                exit 1
                ;;
            esac
          done <<< "$changed"

          if [ "$mode" = "current-pr" ] || [ "$mode" = "merge-sync-push" ]; then
            expected="$(printf '%s\n' \
              '.github/workflows/v2-book-game-content.yml' \
              'acs/book_game_content.py' \
              'tests/test_v2_book_game_content.py' | sort)"
            test "$changed" = "$expected"
          fi

          git diff --quiet "$upstream" HEAD -- \
            acs/gametree.py \
            acs/bookdocument.py \
            acs/bookreader.py \
            acs/book_index.py \
            acs/book_training.py \
            acs/training.py \
            acs/book_webview_bridge.py \
            acs/book_webview_projection.py \
            acs/full_product_presenters.py \
            acs/acsdb.py \
            acs/chesscore.py \
            acs/analysis_service.py \
            acs/engine_assisted_workflows.py \
            web/full_product_books_training.js \
            acs/stage1_release_ui.py

      - name: Compile Book canonical content package
        run: >-
          python -m compileall -q
          acs/book_game_content.py
          tests/test_v2_book_game_content.py

      - name: Focused Book GameTree regressions
        run: >-
          python -m unittest -v
          tests.test_v2_book_game_content
          tests.test_gametree
          tests.test_bookdocument
          tests.test_v2_book_progress_store

      - name: Full repository unittest
        run: python -m unittest discover -s tests -v

      - name: Full repository pytest
        run: python -m pytest -q tests

      - name: SELFTEST and complete diagnostic
        run: |
          python -m acs.selftest
          python run_accessible_chess.py --diagnostic
