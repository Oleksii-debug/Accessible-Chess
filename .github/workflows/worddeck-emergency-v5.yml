name: WordDeck historical archive - emergency v5

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical WordDeck emergency v5 is archived read-only. Original logic remains in Git history."
