name: Version 2 Windows NVDA UI

on:
  push:
    branches:
      - work/v2-windows-nvda-ui-20260828
  pull_request:
    branches:
      - work/version2-integration-formats-20260827
    paths:
      - 'acs/version2_windows_file_workflows.py'
      - 'acs/version2_windows_pgn_export.py'
      - 'acs/version2_windows_import_event_mailbox.py'
      - 'acs/version2_windows_import_ui_pump.py'
      - 'acs/version2_windows_native_dialog_ownership.py'
      - 'tests/test_v2_windows_nvda_file_workflows.py'
      - 'tests/test_v2_windows_pgn_export.py'
      - 'tests/test_v2_windows_import_event_mailbox.py'
      - 'tests/test_v2_windows_import_ui_pump.py'
      - 'tests/test_v2_windows_native_dialog_ownership.py'
      - '.github/workflows/v2-windows-nvda-ui.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: v2-windows-nvda-ui-${{ github.ref }}
  cancel-in-progress: true

env:
  V2_UI_BASE: 4b23c5c5a47835e66f886df8b6705cdb535f6531

jobs:
  file-workflow-accessibility:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2025]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Preserve exact Git bytes
        shell: bash
        run: |
          set -euo pipefail
          git config core.autocrlf false
          git config core.eol lf
          git reset --hard HEAD

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install pinned verification dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -q pytest==8.4.2
          if [ "${{ runner.os }}" = "Windows" ]; then
            python -m pip install -q pythonnet==3.0.5
          fi

      - name: Prove exact Version 2 UI ancestry scope and Stage 1 isolation
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor "$V2_UI_BASE" HEAD
          test "$(git merge-base "$V2_UI_BASE" HEAD)" = "$V2_UI_BASE"
          git diff --check "$V2_UI_BASE" HEAD
          changed="$(git diff --name-only "$V2_UI_BASE" HEAD | sort)"
          expected="$(printf '%s\n' \
            '.github/workflows/v2-windows-nvda-ui.yml' \
            'acs/version2_windows_file_workflows.py' \
            'acs/version2_windows_import_event_mailbox.py' \
            'acs/version2_windows_import_ui_pump.py' \
            'acs/version2_windows_native_dialog_ownership.py' \
            'acs/version2_windows_pgn_export.py' \
            'tests/test_v2_windows_import_event_mailbox.py' \
            'tests/test_v2_windows_import_ui_pump.py' \
            'tests/test_v2_windows_native_dialog_ownership.py' \
            'tests/test_v2_windows_nvda_file_workflows.py' \
            'tests/test_v2_windows_pgn_export.py' | sort)"
          test "$changed" = "$expected"
          git diff --quiet "$V2_UI_BASE" HEAD -- \
            acs/chesscore.py \
            acs/gametree.py \
            acs/pgn_document.py \
            acs/pgn_service.py \
            acs/pgn_workspace.py \
            acs/pgn_workspace_webview_adapter.py \
            acs/full_product_actions.py \
            acs/acsdb.py \
            acs/library_import_service.py \
            acs/library_webview_bridge.py \
            acs/library_webview_projection.py \
            acs/chessbase_decoder.py \
            acs/chessbase_library_import.py \
            acs/cbv_extractor.py \
            acs/full_product_native_menu.py \
            acs/stage1_release_ui.py \
            acs/stage1_release_ui_core.py \
            web/stage1_release_bootstrap.js \
            web/stage1_board_actions.js \
            tools/qa

      - name: Compile host composition and tests
        run: >-
          python -m compileall -q
          acs/version2_windows_file_workflows.py
          acs/version2_windows_import_event_mailbox.py
          acs/version2_windows_import_ui_pump.py
          acs/version2_windows_native_dialog_ownership.py
          acs/version2_windows_pgn_export.py
          tests/test_v2_windows_nvda_file_workflows.py
          tests/test_v2_windows_import_event_mailbox.py
          tests/test_v2_windows_import_ui_pump.py
          tests/test_v2_windows_native_dialog_ownership.py
          tests/test_v2_windows_pgn_export.py

      - name: Focused Version 2 Windows file workflow journeys
        run: >-
          python -m unittest -v
          tests.test_v2_windows_nvda_file_workflows
          tests.test_v2_windows_import_event_mailbox
          tests.test_v2_windows_import_ui_pump
          tests.test_v2_windows_native_dialog_ownership
          tests.test_v2_windows_pgn_export
          tests.test_pgn_document
          tests.test_dev1_pgn_workspace_webview_adapter
          tests.test_dev1_full_product_accessible_shell
          tests.test_dev1_full_product_ui_packages
          tests.test_dev1_full_product_webview_adapter
          tests.test_library_import_ui_integration
          tests.test_version2_formats_integration

      - name: Windows WinForms file-dialog owner and UI-post type composition smoke
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from acs.version2_windows_file_workflows import Version2WindowsFileDialogs
          from acs.version2_windows_import_ui_pump import Version2WinFormsUiPoster
          from acs.version2_windows_native_dialog_ownership import (
              Version2OwnedWindowsFileDialogs,
              Version2OwnedWindowsPgnExportDialogs,
          )
          from acs.version2_windows_pgn_export import Version2WindowsPgnExportDialogs

          DialogResult, OpenFileDialog, SaveFileDialog = Version2WindowsFileDialogs._load_forms()
          from System.Windows.Forms import Control
          export_result, _, ExportSaveFileDialog = Version2WindowsPgnExportDialogs._load_forms()
          opened = OpenFileDialog()
          saved = SaveFileDialog()
          exported = ExportSaveFileDialog()
          owner = Control()
          owned_files = Version2OwnedWindowsFileDialogs(lambda: owner)
          owned_export = Version2OwnedWindowsPgnExportDialogs(lambda: owner)
          try:
              opened.Multiselect = False
              opened.CheckFileExists = True
              saved.AddExtension = True
              saved.OverwritePrompt = True
              exported.AddExtension = True
              exported.OverwritePrompt = True
              poster = Version2WinFormsUiPoster(owner)
              delegate = poster._system_action(lambda: None)
              assert callable(owner.BeginInvoke)
              assert delegate is not None
              assert owned_files.dialog_owner.resolve() is owner
              assert owned_export.dialog_owner.resolve() is owner
              assert opened.Multiselect is False
              assert opened.CheckFileExists is True
              assert saved.AddExtension is True
              assert saved.OverwritePrompt is True
              assert exported.AddExtension is True
              assert exported.OverwritePrompt is True
              assert DialogResult.OK is not None
              assert export_result.OK is not None
          finally:
              owner.Dispose()
              opened.Dispose()
              saved.Dispose()
              exported.Dispose()
          print('WINDOWS WINFORMS FILE DIALOG OWNER + UI POST COMPOSITION PASS')
          PY

      - name: Full repository unittest
        run: python -m unittest discover -s tests -v

      - name: Full repository pytest
        run: python -m pytest -q tests

      - name: SELFTEST and complete diagnostic
        run: |
          python -m acs.selftest
          python run_accessible_chess.py --diagnostic
